apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-nodejs.fullname" . }}
  labels:
    {{- include "helm-chart-nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-nodejs.selectorLabels" . | nindent 4 }}
